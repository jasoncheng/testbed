apiVersion: v1
kind: Service
metadata:
  namespace: "{{ K8S_DEFAULE_NAMESPACE }}"
  name: hello-service-v2
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: hello-app-v2
  type: LoadBalancer
  #type: NodePort
